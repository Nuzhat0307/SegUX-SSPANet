import { useState,useEffect } from 'react'
import { Link } from 'react-router-dom'
import { Upload,Users,Scan,TrendingUp,AlertTriangle,Activity,ChevronRight } from 'lucide-react'
import { supabase } from '../lib/supabase'
import { useAuth } from '../lib/auth'
import PageHeader from '../components/PageHeader'
import PredictionCard from '../components/PredictionCard'
import { PredictionResult,Patient,TumorClass,TUMOR_CLASS_COLORS,TUMOR_CLASS_DISPLAY } from '../lib/types'
interface Stats{total:number;patients:number;uncertain:number;confidence:number}
type Recent=Pick<PredictionResult,'id'|'patient_id'|'predicted_class'|'predicted_class_display'|'uncertainty'|'model_version'|'inference_time_ms'|'created_at'>
export default function Dashboard(){const{user}=useAuth();const[stats,setStats]=useState<Stats>({total:0,patients:0,uncertain:0,confidence:0});const[recent,setRecent]=useState<Recent[]>([]);const[patients,setPatients]=useState<Patient[]>([]);const[distribution,setDistribution]=useState<Record<string,number>>({glioma:0,meningioma:0,pituitary:0,no_tumor:0});const[loading,setLoading]=useState(true);useEffect(()=>{let active=true;(async()=>{try{const[{data:rd,error:re},{data:pd,error:pe},{data:summary,error:se}]=await Promise.all([supabase.from('predictions').select('id,patient_id,predicted_class,predicted_class_display,uncertainty,model_version,inference_time_ms,created_at').order('created_at',{ascending:false}).limit(5),supabase.from('patients').select('id,name,age,gender,mrn,notes,created_at').order('created_at',{ascending:false}).limit(5),supabase.from('predictions').select('patient_id,predicted_class,uncertainty')]);if(re)throw re;if(pe)throw pe;if(se)throw se;if(!active)return;const rows=(summary||[])as any[];const dist={glioma:0,meningioma:0,pituitary:0,no_tumor:0}as Record<string,number>;let uncertain=0,conf=0;const counts=new Map<string,number>();rows.forEach(p=>{if(dist[p.predicted_class]!==undefined)dist[p.predicted_class]++;if(p.uncertainty?.is_uncertain)uncertain++;conf+=Number(p.uncertainty?.confidence||0);counts.set(p.patient_id,(counts.get(p.patient_id)||0)+1)});setRecent((rd||[])as Recent[]);setPatients((pd||[]).map((p:any)=>({...p,prediction_count:counts.get(p.id)||0})));setDistribution(dist);setStats({total:rows.length,patients:(pd||[]).length,uncertain,confidence:rows.length?conf/rows.length:0})}catch(e){console.error('Dashboard load error',e)}finally{if(active)setLoading(false)}})();return()=>{active=false}},[]);const cards=[['Total Scans Analyzed',stats.total,Scan,'bg-primary-100','text-primary-600'],['Total Patients',stats.patients,Users,'bg-secondary-100','text-secondary-600'],['Cases Needing Review',stats.uncertain,AlertTriangle,'bg-warning-100','text-warning-600'],['Average Confidence',`${(stats.confidence*100).toFixed(1)}%`,TrendingUp,'bg-success-100','text-success-600']]as const;return <div><PageHeader title="Dashboard" description={`Welcome back${user?.user_metadata?.full_name?`, ${user.user_metadata.full_name}`:''}. Here's your diagnostic overview.`} action={<Link to="/upload" className="btn-primary"><Upload className="h-4 w-4"/>New Scan</Link>}/><div className="mb-8 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([l,v,I,b,c])=><div key={l} className="card p-5"><div className="flex items-center justify-between"><div><p className="text-sm text-neutral-500">{l}</p><p className="mt-1 text-2xl font-bold">{v}</p></div><div className={`flex h-12 w-12 items-center justify-center rounded-xl ${b}`}><I className={`h-6 w-6 ${c}`}/></div></div></div>)}</div><div className="grid grid-cols-1 gap-6 lg:grid-cols-3"><div className="lg:col-span-2 card p-5"><div className="mb-4 flex justify-between"><h2 className="text-lg font-semibold">Recent Analyses</h2><Link to="/history" className="flex items-center text-sm text-primary-600">View all<ChevronRight className="h-4 w-4"/></Link></div>{loading?<div className="space-y-3">{[1,2,3].map(i=><div key={i} className="h-24 rounded-lg shimmer-bg"/>)}</div>:recent.length?<div className="space-y-3">{recent.map(p=><PredictionCard key={p.id} prediction={p as PredictionResult}/>)}</div>:<div className="py-12 text-center"><Activity className="mx-auto mb-3 h-10 w-10 text-neutral-400"/><p>No analyses yet</p><Link to="/upload" className="btn-primary mt-4"><Upload className="h-4 w-4"/>Upload MRI</Link></div>}</div><div className="space-y-6"><div className="card p-5"><h2 className="mb-4 text-lg font-semibold">Tumor Distribution</h2>{Object.entries(distribution).map(([k,n])=>{const total=Math.max(1,Object.values(distribution).reduce((a,b)=>a+b,0));return <div key={k} className="mb-3"><div className="mb-1 flex justify-between text-xs"><span>{TUMOR_CLASS_DISPLAY[k as TumorClass]}</span><span>{n}</span></div><div className="h-2 rounded-full bg-neutral-100"><div className="h-full rounded-full" style={{width:`${n/total*100}%`,backgroundColor:TUMOR_CLASS_COLORS[k as TumorClass]}}/></div></div>})}</div><div className="card p-5"><h2 className="mb-4 text-lg font-semibold">Recent Patients</h2>{patients.length?patients.map((p:any)=><Link key={p.id} to={`/history?patient=${p.id}`} className="flex items-center justify-between rounded-lg px-3 py-2.5 hover:bg-neutral-50"><div><p className="text-sm font-medium">{p.name}</p><p className="text-xs text-neutral-400">MRN: {p.mrn}</p></div><span className="badge bg-primary-50 text-primary-600">{p.prediction_count} scans</span></Link>):<p className="py-8 text-center text-sm text-neutral-400">No patients registered yet</p>}</div></div></div></div>}
